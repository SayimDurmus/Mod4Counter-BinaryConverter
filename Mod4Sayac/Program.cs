using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.NetworkInformation;
using System.Text;
using System.Threading.Tasks;

namespace Mod4Sayac
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // 3 basamaklı mod4 sayacının yapılması
            //int sayi = 100;
            //double birlerBas, dortlerBas, onaltilarBas, toplam;

            //for (int i = 1; i < sayi; i++)
            //{
            //    birlerBas = (i % 4);
            //    dortlerBas = ((((i - birlerBas) / 4) % 4));
            //    onaltilarBas = (((((i - birlerBas) / 4) - dortlerBas) / 4));
            //    toplam = birlerBas * Math.Pow(4, 0) + dortlerBas * Math.Pow(4, 1) + onaltilarBas * Math.Pow(4, 2);
            //    string sonuc = $"{onaltilarBas}{dortlerBas}{birlerBas}";
            //    string msg = $"{onaltilarBas}{dortlerBas}{birlerBas} -- {toplam}";
            //    if (sonuc == "400")
            //    {
            //        break;
            //    }

            //    Console.WriteLine(msg);
            //    birlerBas = 0;
            //    dortlerBas = 0;
            //    onaltilarBas = 0;
            //    sonuc = string.Empty;

            //}
            //Console.ReadLine();

            

        }
    }
}
